1. `cargo add tokio --features macros,rt-multi-thread` — Async runtime for `ashpd` portal calls
2. `cargo add ashpd` — xdg-desktop-portal D-Bus interface for Wayland screenshot capture
3. `cargo add chrono` — Timestamp generation for default filenames
5. `cargo add image --features avif,avif-native,png,jpeg,webp` — WEBP/PNG/JPEG/AVIF encoding and image manipulation
6. `cargo add thiserror` — Derive macro for custom error types
7. `cargo add dirs` — Locate default save directory (e.g., `~/Pictures`)
8. `cargo add lexopt` — Minimal argv token scanner for custom CLI parser
